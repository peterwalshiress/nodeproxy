{
  "name": "proxy",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "repository": "",
  "license": "MIT",
  "devDependencies": {
  },
  "scripts": {

    "prestart": "npm install",
    "start": "node proxy.js",
    
    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update"
  }
}
